
// See https://github.com/karussell/jsii/blob/master/web/js/src/JSii.js
(function ($) {
    $.fn.contentRoles = function (option, settings) {
        /*
        * For each element
        * Run the classifier/ranker
        * Use Readability scores to find text
        * Guess if it's advertising, navigation, structure, body, note, link, image, object, video
        * Give the guess a score
        *
        *
        *
        *
        * */
    }

    $.fn.parse = function(option, settings) {

    }

    $.fn.tagWords = function(option, settings) {

    }
    $.fn.tagStatements = function(option, settings) {

    }
    $.fn.index = function(option, settings) {
        for word in words
            for char in word
                index[char]
    }

    $.fn.rank = function(option, settings) {

    }

}(jQuery);